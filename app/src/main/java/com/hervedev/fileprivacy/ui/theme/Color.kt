package com.hervedev.fileprivacy.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Accent Colors
val PrimaryLight = Color(0xFF1259C3)
val PrimaryDark = Color(0xFF4D9DFF)

// Light Accent Background (Primary Container)
val PrimaryBgLight = Color(0xFFE8EFFC)
val PrimaryBgDark = Color(0xFF0E1E36)

// Screen Backgrounds
val BackgroundLight = Color(0xFFF2F2F7)
val BackgroundDark = Color(0xFF1C1C1E)

// Surfaces / Cards
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF2C2C2E)

// Text Colors
val TextMainLight = Color(0xFF1C1C1E)
val TextSecondaryLight = Color(0xFF6E6E73)
val TextTertiaryLight = Color(0xFFABABAB)

val TextMainDark = Color(0xFFFFFFFF)
val TextSecondaryDark = Color(0xFF9B9BA0)
val TextTertiaryDark = Color(0xFF545458)

// Borders & Dividers
val BorderLight = Color(0xFFE5E5EA)
val DividerLight = Color(0xFFF0F0F0)

val BorderDark = Color(0xFF3A3A3C)
val DividerDark = Color(0xFF2C2C2E)

// Functional Colors
val ErrorRed = Color(0xFFFF3B30)
val SuccessGreen = Color(0xFF34C759)
val WarningOrange = Color(0xFFFF9500)

// Pastel Badges (Accent + Background)
object FileTypeBadges {
    // Folder & Archive
    val FolderAccent = Color(0xFFFF9F0A)
    val FolderBg = Color(0xFFFFF4E0)

    // Image
    val ImageAccent = Color(0xFF4D9DFF)
    val ImageBg = Color(0xFFE5EFFF)

    // Video
    val VideoAccent = Color(0xFFFF453A)
    val VideoBg = Color(0xFFFFE9E8)

    // Audio
    val AudioAccent = Color(0xFFBF5AF2)
    val AudioBg = Color(0xFFF3E5FF)

    // Document
    val DocumentAccent = Color(0xFF32D74B)
    val DocumentBg = Color(0xFFE3F9E8)

    // APK
    val ApkAccent = Color(0xFF64D2FF)
    val ApkBg = Color(0xFFE0F5FF)
}
